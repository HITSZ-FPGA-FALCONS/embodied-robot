# Contributing Guide

本仓库为三人竞赛团队协作仓库。目标不是追求“大厂式流程”，而是用最少流程降低并行开发、集成与现场调试风险。

## 1. main 分支规则

`main` 表示当前团队认可的稳定基线：

- 能编译 / 综合的代码不要被随意破坏；
- 已经用于演示的版本优先打 tag；
- 大改动不要直接推 main；
- 不允许为了“先传上去”把无法解释的大量生成代码直接合入。

## 2. 开发分支

从最新 `main` 创建短分支：

```bash
git switch main
git pull
git switch -c feat/encoder-counter
```

完成一个清晰交付物后尽快合并，不长期保留“个人大分支”。

推荐命名：

```text
feat/*
fix/*
exp/*
docs/*
chore/*
```

## 3. 一个 Issue = 一个可验收结果

错误示例：

> 学 FPGA

更好的 Issue：

> 完成正交编码器计数 RTL 与 testbench

验收标准：

- A/B 相正转计数增加；
- 反转计数减少；
- 波形截图存档；
- 顶层接口写入文档；
- 通过至少一个异常边界测试。

## 4. Pull Request

PR 不要求写论文，但必须回答：

- 改了什么？
- 为什么改？
- 怎么验证？
- 是否影响接口/硬件？
- 还有什么已知问题？

核心 FPGA / 电机 / 安全控制 PR 尽量由另一名队员 review。

## 5. FPGA 开发顺序

```text
接口定义
→ RTL
→ Testbench
→ 波形
→ 综合/实现
→ 上板
→ 逻辑分析仪/实测
→ 集成
```

不能用“上板偶尔能跑”替代模块验证。

## 6. 实验性代码

不确定是否采用的算法、视觉方案或第三方项目：

- 优先用 `exp/*` 分支；
- 若规模较大或依赖体系完全不同，建立独立仓库；
- 最终进入主项目时，只合入被验证需要的部分。

## 7. Fork 规则

官方例程 / 开源项目：

- 优先 Fork 到 Organization；
- 保留 upstream；
- 不把整个 Fork 再复制进主仓库；
- 如最终使用，记录固定 commit；
- 修改尽量在 Fork 自己的分支中进行。

## 8. 禁止事项

- 三个人同时修改同一个顶层文件但不提前沟通；
- 把开发板生成的大量缓存/临时文件全提交；
- 只发代码不写接线和验证方法；
- 重要参数只存在聊天记录里；
- 为赶进度绕过版本控制直接互传“final_v7_真的最终版.zip”。

## 9. Definition of Done

一个任务只有满足以下条件才算 Done：

1. 输出物已提交；
2. 能复现；
3. 验收标准通过；
4. 必要文档已更新；
5. 不依赖“只有作者自己知道”的隐藏步骤。
